<%= form_for :session, :url => login_path, :method => :post do |f| %>

	<%= f.label "Email address" %>
	<%= f.text_field :email %>

	<br>
	<%= f.label "Password" %>
	<%= f.text_field :password %>


	<div class="actions">
		<br/>
		<%= f.submit "Log in" %>
	</div>

<% end %>